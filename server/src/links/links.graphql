type Link {
  id: String
  link: String
  title: String
  description: String
  likeCount: Int
  createdAt: String
  createdBy: String
}

input AddLinkInput {
  link: String!
}

type Query {
  getLinks(page: Int, limit: Int): [Link]
}

type Mutation {
  addLink(addLinkInput: AddLinkInput): Link
}
